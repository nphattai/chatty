version: 3.8;

services:
  dev-db:
    image: postgres:13
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=chatty
    networks:
      - chatty-network
networks:
  chatty-network:
